{
  "name": "mind-saathi-admin",
  "private": true,
  "scripts": {
    "all": "npm run dev",
    "dev": "concurrently -n api,web -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix client run dev",
    "start": "concurrently -n api,web -c green,cyan \"npm --prefix server run start\" \"npm --prefix client run dev\""
  },
  "dependencies": {
    "twilio": "^5.10.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
